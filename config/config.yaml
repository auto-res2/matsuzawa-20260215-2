# Main configuration for Hydra
# CPV-CoT: Cross-Perspective Verification Chain-of-Thought experiment

defaults:
  - _self_
  - run: ???  # loads from config/run/, accessible as 'run=' in CLI

# Execution mode: main, sanity_check, or pilot
mode: main

# Results output directory
results_dir: .research/results

# Cache directory for models and datasets
cache_dir: .cache

# WandB configuration
wandb:
  entity: airas
  project: 2026-02-15-2
  mode: online
